name: Schedule Workflow

on: 
  schedule:
    - cron: "50 7 * * *"

jobs:
  run-script: 
    runs-on: ubuntu-latest

    steps:
      - name: print message
        run: echo "Schedule Workflow Executed"
